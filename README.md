# Ward Record Book

Sacrament meeting minutes tracker built with React + Supabase.

## Local development
npm install
npm run dev

## Deploy
Push this repo to GitHub, then import it in Vercel (vercel.com/new).
Vercel auto-detects Vite and deploys with default settings.
